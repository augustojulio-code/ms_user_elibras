spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: root
        password: root
    
    jpa:
        hibernate:
            ddl-auto: create-drop
    
    h2:
        console:
            enable: true
